[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "/bin/bash start.sh"
restartPolicyType = "ON_FAILURE"
healthcheckPath = ""
healthcheckTimeout = 100

[env]
TELEGRAM_BOT_TOKEN = ""
TELEGRAM_CHAT_ID = ""