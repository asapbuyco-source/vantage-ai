[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "/bin/bash start.sh"
restartPolicyType = "ON_FAILURE"
healthcheckPath = ""
healthcheckTimeout = 100

[env]
TELEGRAM_BOT_TOKEN = ""
TELEGRAM_CHAT_ID = ""
# Residential proxy (optional but recommended for 1xbet): http://user:pass@host:port
ARB_PROXY = ""