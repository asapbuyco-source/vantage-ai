fix: CRITICAL crash in report() — screenshotHintForLeg(s) missing the candidate arg

The Brentford-Chelsea alert crashed the process EVERY cycle:
  TypeError: Cannot read properties of undefined (reading 'teams')
  at screenshotHintForLeg -> report -> scan
The alert was sent to Telegram, then the crash killed the loop before screenshots;
Railway restarted the process on each failure, and each restart hit the stale
Xvfb :99 lock ("Server is already active for display 99"). This also explains the
missing screenshots — the screenshot phase was never reached.

- report() now passes c: screenshotHintForLeg(s, c)
- startsWith checks made case-insensitive (bet labels vs c.teams)
- start.sh: pkill stale Xvfb + rm /tmp/.X99-lock before starting (crash-restart safe)
Verified: hints return correct market/line/index for AH home/away, OU, DC; 42/42 tests.